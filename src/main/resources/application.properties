# Server configuration
server.port=8080

# Spring Profile Setup - This automatically pulls configs from application-local.properties
spring.profiles.include=local

# Fallback default value for git tracking
gemini.api.key=YOUR_GEMINI_API_KEY_HERE